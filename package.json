{
  "name": "ebnf-highlighter",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.6.1"
  },
  "dependencies": {
    "ebnf": "^1.2.0",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "(rm -fr gh-pages || true) && react-scripts build && mv build gh-pages",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
